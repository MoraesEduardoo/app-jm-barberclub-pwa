self.addEventListener("push", t => { let i = {}; try { i = t.data ? t.data.json() : {} } catch (n) { i = { title: "JM Barberclub", body: t.data ? t.data.text() : "" } } let n = i.title || "JM Barberclub", e = { body: i.body || "", icon: i.icon || "/icons/logo-192.png", badge: i.badge || "/icons/logo-192.png", data: { url: i.url || "/admin" }, tag: i.tag || void 0, renotify: !1, vibrate: [120, 60, 120] }; t.waitUntil(self.registration.showNotification(n, e)) }), self.addEventListener("notificationclick", t => { var i; t.notification.close(); let n = (null === (i = t.notification.data) || void 0 === i ? void 0 : i.url) || "/admin"; t.waitUntil(self.clients.matchAll({ type: "window", includeUncontrolled: !0 }).then(t => { for (let i of t) if (i.url.includes(n) && "focus" in i) return i.focus(); if (self.clients.openWindow) return self.clients.openWindow(n) })) });